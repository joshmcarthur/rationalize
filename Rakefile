begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "rationalize"
    gem.summary = "A plug-and-play Redis-backed web frontend for I18N translations."
    gem.email = "josh@3months.com"
    gem.authors = ["Josh McArthur"]
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{public}/**/*", "{config}/**/*"]
  end
  Jeweler::GemcutterTasks.new
rescue
  puts "Jeweler or dependency not available."
end
